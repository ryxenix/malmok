bin/
out/

# docs/img 에는 README 가 참조하는 것만 둔다.
# scripts/record.sh 로 재생성한다 — 손으로 찍어 넣지 않는다.
docs/img/*
!docs/img/tui-*.gif
!docs/img/malmok-wordmark.png
dist/
site/
internal/tools/payload/

# Bytecode left by the MkDocs hook in scripts/ on every local build.
__pycache__/
