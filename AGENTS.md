# Repository working agreements

- After completing and verifying a requested change, commit the task's changes.
  Preserve unrelated work and do not include it in the commit.
- Do not push unless the user explicitly requests it.
- Keep the repository private until the user explicitly authorizes a visibility change.
  Do not publish documentation or trigger deployment without explicit authorization.
