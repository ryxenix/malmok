# 진단 코드

실행 결과의 코드로 아래 표에서 진단 내용을 찾으십시오. `PF-*`는 사전 점검,
`EX-*`는 실행 오류, `UP-*`는 업그레이드 관련 코드입니다.

이 표는 코드에서 생성한 공통 레지스트리입니다. **진단 메시지 원문은 영어로
유지**하여 CLI 로그와 정확하게 대조할 수 있게 했습니다. 표의 `block`은 진행을
막는 조건, `warn`은 검토할 경고를 뜻합니다. 폐기된 번호는 재사용하지 않습니다.

--8<-- "docs/99-codes.md"
